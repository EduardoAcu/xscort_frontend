export const metadata = {
  title: "Restablecer Contraseña | xscort",
  description: "Crea una nueva contraseña segura para tu cuenta.",
  robots: {
    index: false,
    follow: false,
  },
};

export default function Layout({ children }) {
  return <>{children}</>;
}